GROQ_API_KEY=
GEMINI_API_KEY=
APP_URL=
LEVELS_BACKEND_URL=

# NOTE: this file is for the root-level AI scripts (ai-services/, levelsBackendClient.ts) only.
# The backend server's own database URL is a SEPARATE file: copy backend/.env.example to
# backend/.env and set MONGODB_URI there (NOT here — "MONGO_URI" at the root is never read
# by any code in this repo, it will silently be ignored).
