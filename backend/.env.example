PORT=5000
NODE_ENV=development

MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/fln_platform?retryWrites=true&w=majority


JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Aadhaar Vault service integration.
# Must match the vault's SERVICE_JWT_HMAC_SECRET / SERVICE_JWT_ISSUER / SERVICE_JWT_AUDIENCE.
AADHAAR_VAULT_URL=http://127.0.0.1:4101
AADHAAR_VAULT_SERVICE_JWT_SECRET=your-vault-shared-hmac-secret
AADHAAR_VAULT_SERVICE_JWT_ISSUER=https://fln-backend.example.invalid
AADHAAR_VAULT_SERVICE_JWT_AUDIENCE=fln-vault
AADHAAR_VAULT_SERVICE_JWT_SUBJECT=fln-backend-service

# How long (milliseconds) the backend waits for a vault tokenize response
# before failing the registration (fail-closed). Optional; default 10000.
# AADHAAR_VAULT_TIMEOUT_MS=10000

# Shared password for the seeded demo accounts (stored only as a bcrypt hash).
# Defaults to the public demo password if unset; set a private value for real deployments.
SEED_DEMO_PASSWORD=Fln@2026

# PDF generation uses headless Chrome via Puppeteer. Install the browser once with:
#   npx puppeteer browsers install chrome
# (plus its system libraries on Linux). Leave CHROME_EXECUTABLE_PATH unset to use
# that managed browser, or point it at a specific Chrome/Chromium binary.
# CHROME_EXECUTABLE_PATH=/usr/bin/chromium
